#!/usr/bin/python3
"""this funciton returns the transpose of a 2D matrix"""


def matrix_transpose(matrix):

    arr = zip(matrix)

    for rows in arr:
        tp_matrix = [list(arr)]

    return (tp_matrix)
